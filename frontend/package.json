{
  "name": "retirement-sim-ui",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest",
    "typecheck": "node node_modules/typescript/bin/tsc --noEmit -p tsconfig.json",
    "check:duplicates": "node scripts/check-duplicates.mjs",
    "check:imports": "node scripts/check-import-boundaries.mjs",
    "perf:report": "node scripts/check-bundle-size.mjs",
    "perf:gate:warn": "node scripts/check-bundle-size.mjs --warn",
    "perf:gate:hard": "node scripts/check-bundle-size.mjs --enforce",
    "perf:gate": "node scripts/check-bundle-size.mjs --enforce",
    "verify:refactor": "npm run check:duplicates && npm run typecheck && npm run check:imports && npm run perf:report",
    "verify:pr": "npm run check:duplicates && npm run typecheck && npm run check:imports && npm run build && npm run perf:gate:warn",
    "verify:ci": "npm run check:duplicates && npm run typecheck && npm run check:imports && npm run build && npm run perf:gate:hard"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "recharts": "^2.12.7"
  },
  "devDependencies": {
    "@types/react": "^18.3.3",
    "@types/react-dom": "^18.3.0",
    "@vitejs/plugin-react": "^4.3.1",
    "typescript": "^5.5.4",
    "vite": "^5.4.2",
    "vitest": "^2.0.5",
    "jsdom": "^24.1.1",
    "@testing-library/react": "^16.0.0"
  }
}
